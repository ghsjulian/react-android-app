import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.my.reactapp',
  appName: 'MyApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
